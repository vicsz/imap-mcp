# spec-001 — Basic MCP

Status: done
Type: feature

## Outcome

The program runs as a local stdio MCP server and exposes the structured `hello` tool.

## Done when

- [x] MCP initialization works.
- [x] Tool discovery exposes `hello`.
- [x] Calling `hello` returns structured JSON.
- [x] `go test ./...` passes.
